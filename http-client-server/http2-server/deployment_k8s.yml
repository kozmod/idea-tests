#Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-http2-server-dep
spec:
  replicas: 2
  selector:
    matchLabels:
      app: go-http2
  minReadySeconds: 15
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: go-http2
    spec:
      containers:
        - image: go-http2-server:v1.0.1
          name: go-http2-server
          ports:
            - containerPort: 8080

# service
apiVersion: v1
kind: Service
metadata:
  name: http2-server-service-lb
spec:
  type: LoadBalancer
  ports:
    - port: 9091
      protocol: TCP
      targetPort: 8080
  selector:
    app: go-http2

#Pod
apiVersion: v1
kind: Pod
metadata:
  name: http2-server
  labels:
  app:go-http2
spec:
  containers:
    - image: go-http2-server:v1.0.0
      name: go-http2-server
      ports:
        - containerPort: 8080