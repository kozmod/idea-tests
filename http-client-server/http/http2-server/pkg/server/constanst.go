package server

const payloadBox = `┌─────────┐
│ Payload │
└─────────┘
`
const headerBox = `┌─────────┐
│ Headers │
└─────────┘
`
const helloBox = `┌─────────┐
│  Hello  │
└─────────┘
`
const (
	pingRq = "ping"
	pongRs = "pong"
	WTF    = "WTF?"
)
